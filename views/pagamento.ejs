<html>
    <body>
        <table>
            <tr>
                <td>id</td>
                <td>nome</td>
                <td>valor</td>
            </tr>                
            <% for(var i=0; i<lista.length; i++) {%>
                <tr>
                    <td><%= lista[i].id %></td>
                    <td><%= lista[i].nome %></td>
                    <td><%= lista[i].valor %></td>
                </tr>
            <% } %>
        </table>
    </body>
</html>